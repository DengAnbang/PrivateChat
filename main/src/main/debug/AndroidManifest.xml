<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abxh.media.audio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.hezheyi.main.App"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name="com.hezheyi.main.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.hezheyi.main.activity.GuiderActivity" />
        <provider
            android:name="com.hezheyi.main.provider.MainContextProvider"
            android:authorities="${applicationId}.contextprovider"
            android:exported="false" />
    </application>
    <!--摄像头权限-->
    <uses-feature android:name="android.hardware.camera.any" />
    <!--具备摄像头-->
    <uses-feature android:name="android.hardware.camera.any" />
    <!--存储图像或者视频权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--录制音频权限-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>